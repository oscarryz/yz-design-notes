```JavaScript
state { check { s String }}
solution: {
  input String
  input.each(state.check)
  n
}
n:0 
first: {
  a String 
  check: {
     s String
     if digit(s) {
        a = s
        state = second
     }
  }
}
second: {
   a String 
   check: {
     s String 
     when[
      {digit(s)}:{ ​a = s }
      {newline(s)}:{
        n=n + int.parse(
         first.a ++ a
        )
        state = first 
      }]
   }
}
state = first
solution(s)
print("Solution: $(n)")
```
    
